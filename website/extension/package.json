{
  "name": "syncflix-extension",
  "version": "0.1.0",
  "private": true,
  "description": "SyncFlix browser extension — controls video playback, driven by the SyncFlix website.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "@types/chrome": "^0.0.268",
    "archiver": "^8.0.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
